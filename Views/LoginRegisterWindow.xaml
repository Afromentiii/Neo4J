<Window x:Class="Neo4J.Views.LoginRegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Authentication"
        Height="700"
        Width="820"
        WindowStartupLocation="CenterScreen"
        Background="Transparent">

    <Window.Resources>
        <Style x:Key="ModernInputStyle" TargetType="Control">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Control">
                        <Border x:Name="MainBorder"
                                BorderBrush="#CCCCCC"
                                BorderThickness="1"
                                CornerRadius="12"
                                Background="White">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="10,8"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="MainBorder" Property="BorderBrush" Value="DarkBlue"/>
                                <Setter TargetName="MainBorder" Property="BorderThickness" Value="1.5"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="MainBorder" Property="BorderBrush" Value="DarkBlue"/>
                                <Setter TargetName="MainBorder" Property="BorderThickness" Value="1.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AnimatedButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="DarkBlue"/>
            <Setter Property="Foreground" Value="WhiteSmoke"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetName="border" Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)" To="Green" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)" To="1.05" Duration="0:0:0.15"/>
                                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)" To="1.05" Duration="0:0:0.15"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetName="border" Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)" To="DarkBlue" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)" To="1" Duration="0:0:0.15"/>
                                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)" To="1" Duration="0:0:0.15"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Foreground" Value="#777777"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="TabBorder" 
                                Margin="5,0" 
                                Padding="25,10" 
                                CornerRadius="18" 
                                Background="Transparent" 
                                BorderThickness="1" 
                                BorderBrush="Transparent"
                                Cursor="Hand">
                            <ContentPresenter x:Name="ContentSite"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Center"
                                            ContentSource="Header">
                                <ContentPresenter.Resources>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="FontSize" Value="14"/>
                                    </Style>
                                </ContentPresenter.Resources>
                            </ContentPresenter>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="TabBorder" Property="Background" Value="#E0E0E0"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="TabBorder" Property="Background" Value="DarkBlue"/>
                                <Setter TargetName="TabBorder" Property="BorderBrush" Value="DarkBlue"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TabControl">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Border Background="#EEEEEE" 
                                    HorizontalAlignment="Center" 
                                    CornerRadius="20" 
                                    Padding="5" 
                                    Margin="0,0,0,20">
                                <StackPanel IsItemsHost="True" Orientation="Horizontal"/>
                            </Border>
                            <ContentPresenter Grid.Row="1" ContentSource="SelectedContent"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="AliceBlue" Offset="0"/>
                <GradientStop Color="PeachPuff" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Border Width="580"
                VerticalAlignment="Center"
                Background="WhiteSmoke"
                CornerRadius="30"
                BorderBrush="#2D2D2D"
                BorderThickness="1"
                Padding="20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="25" ShadowDepth="8" Opacity="0.15"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,10,0,25">
                    <TextBlock Text="Movie Application"
                               FontSize="26"
                               FontWeight="Bold"
                               Foreground="#2D2D2D"
                               HorizontalAlignment="Center"/>

                </StackPanel>

                <TabControl Grid.Row="1">

                    <TabItem x:Name="LoginBtn" Header="LOGIN">
                        <StackPanel Margin="30,10">
                            <TextBlock Text="Username" Margin="5,0,0,5" FontWeight="SemiBold" Foreground="#444"/>
                            <TextBox Text="" x:Name="LoginUsername" Height="42" Style="{StaticResource ModernInputStyle}" TextChanged="LoginUsername_TextChanged"/>

                            <TextBlock Text="Password" Margin="5,15,0,5" FontWeight="SemiBold" Foreground="#444"/>
                            <PasswordBox Password="" x:Name="LoginPassword" Height="42" Style="{StaticResource ModernInputStyle}"/>

                            <Button Content="Log In"
                                    Height="50"
                                    Margin="0,35,0,10"
                                    Style="{StaticResource AnimatedButtonStyle}"
                                    Click="BtnLogin_Click"/>
                        </StackPanel>
                    </TabItem>

                    <TabItem x:Name="RegisterBtn" Header="REGISTER">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="450">
                            <StackPanel Margin="30,10">
                                <TextBlock Text="Username" Margin="5,0,0,5" FontWeight="SemiBold" Foreground="#444"/>
                                <TextBox x:Name="RegUsername" Height="40" Style="{StaticResource ModernInputStyle}"/>

                                <Grid Margin="0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="15"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="First Name" Margin="5,0,0,5" FontWeight="SemiBold" Foreground="#444"/>
                                        <TextBox x:Name="RegFirstName" Height="40" Style="{StaticResource ModernInputStyle}"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Last Name" Margin="5,0,0,5" FontWeight="SemiBold" Foreground="#444"/>
                                        <TextBox x:Name="RegLastName" Height="40" Style="{StaticResource ModernInputStyle}"/>
                                    </StackPanel>
                                </Grid>

                                <TextBlock Text="Email" Margin="5,0,0,5" FontWeight="SemiBold" Foreground="#444"/>
                                <TextBox x:Name="RegEmail" Height="40" Style="{StaticResource ModernInputStyle}"/>

                                <TextBlock Text="Password" Margin="5,10,0,5" FontWeight="SemiBold" Foreground="#444"/>
                                <PasswordBox x:Name="RegPassword" Height="40" Style="{StaticResource ModernInputStyle}"/>

                                <TextBlock Text="Confirm Password" Margin="5,10,0,5" FontWeight="SemiBold" Foreground="#444"/>
                                <PasswordBox x:Name="RegConfirmPassword" Height="40" Style="{StaticResource ModernInputStyle}"/>

                                <Button Content="Create Account"
                                        Height="50"
                                        Margin="0,25,0,10"
                                        Style="{StaticResource AnimatedButtonStyle}"
                                        Click="BtnRegister_Click"/>
                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>
                </TabControl>
            </Grid>
        </Border>
    </Grid>
</Window>